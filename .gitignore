.venv
**/__pycache__
**/*.pyc

# secrets / login helpers (contain tokens)
hf_token.txt
hf_login.sh
wandb_login.sh

# raw data (83 GB: the CJVT KG zip + extracted N-Triples) — never commit
data/kg_raw/

# training / experiment artifacts
checkpoints
wandb
job_logs/
**/*.pth
*.gtds/
*.gtds.lock
.inductor_cache/
**/_inductor_cache/

# packaging
*.egg-info/
build/
dist/

# editor / OS
.DS_Store

# assistant context (private)
CLAUDE_CONTEXT.md

# persisted v3 graph store (3.8 GB artifact, rebuildable from kg_raw in ~15 min)
data/kg_graph_v3/

# the lexicographical QA dataset -- an internal sample, NOT public (see README
# "Data").  Committing it would publish it via GitHub.
data/Lexical-QA-SLO*

# v3.1 graph store (same 3.8 GB artifact class as kg_graph_v3)
data/kg_graph_v3_1/

CLAUDE.md
